$kind: http-request
name: Get Me
url: "{{base_url}}/auth/me"
method: GET
headers:
  Authorization: Bearer faketoken123
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1501777797772458
